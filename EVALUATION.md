# OfferBoast 岗位雷达 — 多角色评估文档

> 评估时间：2026-07-24  
> 评估范围：当前已上线功能（最新岗位 + 链接查时间）  
> 评估方法：代码审查 + 功能走查

---

## 一、产品经理视角

| # | 问题 | 严重度 | 说明 |
|---|------|--------|------|
| P1 | 表格「招聘类型」列硬编码为"正式" | 高 | 代码写死 `<div class="cell">正式</div>`，未使用 `j._recruit_type`，校招岗位也显示"正式"，数据不准确 |
| P2 | 招聘类型筛选缺"实习"选项 | 中 | hero 标签写了"社招 · 校招 · 实习"，但筛选器和类型列都不支持实习 |
| P3 | 公司拉取失败无提示 | 中 | 后端 `errors` 字段返回了失败公司，但前端未展示，用户不知道少了哪家 |
| P4 | 链接查时间结果缺岗位信息 | 低 | 结果只显示标题+发布时间+建议，未展示地点、部门等已有数据 |

## 二、前端工程师视角

| # | 问题 | 严重度 | 说明 |
|---|------|--------|------|
| F1 | 行点击全表重渲染 | 中 | `renderTable(jobs)` 在每次点击行时重建整个 DOM，仅为了切换 active 样式，应只更新 class |
| F2 | 职类分类规则缺陷 | 中 | "产品运营"标题会被分到"运营"而非"产品"——产品规则中 `/产品运营.*产品/` 要求"产品运营"后还有"产品"，不匹配纯"产品运营"标题 |
| F3 | 统计卡片加载无骨架 | 低 | 数字从"-"直接跳到实际值，无过渡，体验略突兀但不影响功能 |
| F4 | 移动端导航标签可能溢出 | 低 | `nav-tabs` 使用 `position: absolute; left: 50%`，在极窄屏幕上可能与品牌名重叠 |

## 三、后端工程师视角

| # | 问题 | 严重度 | 说明 |
|---|------|--------|------|
| B1 | 阿里 fallback 使用 subprocess | 低 | 其他公司走直连 API，阿里 fallback 调 `parsers/alibaba.py` 子进程，启动开销略大但可接受 |
| B2 | 腾讯 fallback 翻页上限 | 低 | 最多翻 5 页×100=500 条，超过 500 条的岗位无法通过链接查时间找到，但已有 10s 超时限制 |
| B3 | 无 API 调用频率限制 | 低 | 高频请求可能导致企业 ATS 封 IP，但当前用户量极小，风险可控 |

## 四、测试工程师视角

| # | 问题 | 严重度 | 说明 |
|---|------|--------|------|
| T1 | 无自动化测试 | 中 | 无单元测试、无接口测试，所有验证靠手动，迭代后回归风险高 |
| T2 | `classifyFunction` 分类准确率未验证 | 中 | 正则规则覆盖面未知，可能存在误分类（如"数据产品经理"→先匹配"产品"而非"数据"） |
| T3 | 链接查时间边界用例 | 低 | 未测试：空 URL、不支持的公司域名、已下架岗位、URL 无 job_id 等场景 |

## 五、用户视角

| # | 问题 | 严重度 | 说明 |
|---|------|--------|------|
| U1 | 首次加载慢 | 中 | 9 家公司并行拉取，冷启动 5-10s，无渐进式加载（先显示已到的公司） |
| U2 | JD 详情无格式化 | 低 | 岗位描述以纯文本段落展示，无列表/标题等结构化排版 |
| U3 | 无法排序 | 低 | 固定按发布时间倒序，无法按公司、城市等维度排序 |

---

## 六、优先级矩阵

### 立即执行（成本极低，5 分钟内）

| 编号 | 问题 | 改动量 | 说明 |
|------|------|--------|------|
| P1 | 招聘类型列硬编码 | 1 行 | `正式` → `${escapeHtml(rt \|\| "未知")}` |
| F2 | 产品运营分类规则 | 1 行 | `/产品运营.*产品/` → `/产品运营/` |

### 近期执行（成本中等，30 分钟内）

| 编号 | 问题 | 改动量 | 说明 |
|------|------|--------|------|
| F1 | 行点击全表重渲染 | ~10 行 | 改为只切换 active class |
| P3 | 公司拉取失败提示 | ~5 行 | 读取 `data.errors` 展示 toast 或角标 |
| P2 | 招聘类型加"实习" | ~3 行 | 筛选器加 option + 分类逻辑 |

### 后续迭代（成本较高或优先级低）

| 编号 | 问题 | 说明 |
|------|------|------|
| T1 | 自动化测试 | 需搭建测试框架，工作量大 |
| U1 | 渐进式加载 | 需重构后端为流式返回 |
| U2 | JD 格式化 | 需解析 JD 结构，各家格式不一 |
| U3 | 多维度排序 | 需增加排序 UI 和逻辑 |
| F4 | 移动端导航溢出 | 需响应式重排 |
| B3 | API 频率限制 | 需引入 rate limiter |
